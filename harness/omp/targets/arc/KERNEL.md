# Frozen physics (ARC fixture)

The evolver must not write this file or `eval/`. Gold / score is the grader.

Maps to live ARC: `eval_runner/score.py` and gold `expect` blocks. Exact token `unchecked` is not paraphrased here.
