# Overlay archive (DGM-lite)

Snapshots of **project** playbook/skills/tools or staging only. Parent pointer + fitness. No automatic mutation loop. Never archive the checker or `system-prompt` files.
