# Loop plan — `M1` — integrate

> Parent: [LOOP_GRAPH.md](../LOOP_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `M1` |
| **Job** | Wire KERNEL/SURFACES/CACD/method index to TargetPort. |
| **Wave** | 3 |
| **Depends on (data)** | previous wave artifacts |
| **Write paths** | `harness/omp/SURFACES.md, harness/omp/KERNEL.md, harness/omp/CACD.md, harness/omp/cacd/catalog.ts, docs/methods/**` |
| **Read paths** | `docs/plans/p5-target-port.md`, `docs/planning/GATE_0.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | lead Stop command (no nested Task in this worker) |
| **Checker model** | inherit |
| **Max rounds** | 3 |
| **State** | [`M1.state.json`](M1.state.json) |
| **Isolation** | path-ownership |
| **Companion skills** | ponytail |

---

## Stop (required)

```text
rg -q 'TargetPort' harness/omp/SURFACES.md && test -f docs/methods/target-port.md
```

Exit 0 = pass.

---

## Objective

Wire KERNEL/SURFACES/CACD/method index to TargetPort.

## Non-goals

- Extra features not in PRODUCT P0
- Nested graph spawn

---

## Contract

**Input:**

```json
{ "node_id": "M1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -q 'TargetPort' harness/omp/SURFACES.md && test -f docs/methods/target-port.md", "exit_code": 1, "findings": [] }
```



---

## Escalate

After 3 failures, stop dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(target-port): D21 ADR and surfaces` | this node | Stop command |

---

## Do not

- Commit from a nested Task
- Write outside write paths
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files. Checker: pass/exit. Lead updates `M1.state.json`.
