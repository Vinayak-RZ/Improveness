# Loop plan — `D1` — docs-out

> Parent: [LOOP_GRAPH.md](../LOOP_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `D1` |
| **Job** | README names the boot command after evidence. |
| **Wave** | 6 |
| **Depends on (data)** | previous wave artifacts |
| **Write paths** | `README.md, docs/EXTENSIVE.md, docs/CLAIM_LEDGER.md` |
| **Read paths** | `docs/plans/p5-target-port.md`, `docs/planning/GATE_0.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | lead Stop command (no nested Task in this worker) |
| **Checker model** | inherit |
| **Max rounds** | 3 |
| **State** | [`D1.state.json`](D1.state.json) |
| **Isolation** | path-ownership |
| **Companion skills** | ponytail |

---

## Stop (required)

```text
rg -q 'TargetPort' README.md && rg -q 'improve-target' README.md
```

Exit 0 = pass.

---

## Objective

README names the boot command after evidence.

## Non-goals

- Extra features not in PRODUCT P0
- Nested graph spawn

---

## Contract

**Input:**

```json
{ "node_id": "D1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -q 'TargetPort' README.md && rg -q 'improve-target' README.md", "exit_code": 1, "findings": [] }
```



---

## Escalate

After 3 failures, stop dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(readme): TargetPort technique` | this node | Stop command |

---

## Do not

- Commit from a nested Task
- Write outside write paths
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files. Checker: pass/exit. Lead updates `D1.state.json`.
