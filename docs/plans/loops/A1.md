# Loop plan — `A1` — D21 ADR

> Parent: [LOOP_GRAPH.md](../LOOP_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `A1` |
| **Job** | Record TargetPort ADR D21. |
| **Wave** | 1 |
| **Depends on (data)** | previous wave artifacts |
| **Write paths** | `DECISIONS.md` |
| **Read paths** | `docs/plans/p5-target-port.md`, `docs/planning/GATE_0.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | lead Stop command (no nested Task in this worker) |
| **Checker model** | inherit |
| **Max rounds** | 3 |
| **State** | [`A1.state.json`](A1.state.json) |
| **Isolation** | path-ownership |
| **Companion skills** | ponytail |

---

## Stop (required)

```text
rg -q 'D21' DECISIONS.md && rg -q 'TargetPort' DECISIONS.md
```

Exit 0 = pass.

---

## Objective

Record TargetPort ADR D21.

## Non-goals

- Extra features not in PRODUCT P0
- Nested graph spawn

---

## Contract

**Input:**

```json
{ "node_id": "A1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "rg -q 'D21' DECISIONS.md && rg -q 'TargetPort' DECISIONS.md", "exit_code": 1, "findings": [] }
```



---

## Escalate

After 3 failures, stop dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(target-port): D21 ADR and surfaces` | this node | Stop command |

---

## Do not

- Commit from a nested Task
- Write outside write paths
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files. Checker: pass/exit. Lead updates `A1.state.json`.
