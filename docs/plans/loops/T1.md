# Loop plan — `T1` — trials

> Parent: [LOOP_GRAPH.md](../LOOP_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `T1` |
| **Job** | Log queued trials. |
| **Wave** | 5 |
| **Depends on (data)** | previous wave artifacts |
| **Write paths** | `docs/planning/T1_TRIALS.md` |
| **Read paths** | `docs/plans/p5-target-port.md`, `docs/planning/GATE_0.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | lead Stop command (no nested Task in this worker) |
| **Checker model** | inherit |
| **Max rounds** | 3 |
| **State** | [`T1.state.json`](T1.state.json) |
| **Isolation** | path-ownership |
| **Companion skills** | ponytail |

---

## Stop (required)

```text
test -f docs/planning/T1_TRIALS.md && rg -q 'Happy' docs/planning/T1_TRIALS.md
```

Exit 0 = pass.

---

## Objective

Log queued trials.

## Non-goals

- Extra features not in PRODUCT P0
- Nested graph spawn

---

## Contract

**Input:**

```json
{ "node_id": "T1", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "test -f docs/planning/T1_TRIALS.md && rg -q 'Happy' docs/planning/T1_TRIALS.md", "exit_code": 1, "findings": [] }
```


## Trial queue (required)

| # | Trial | Pass means |
|---|-------|------------|
| 1 | Happy P0 path | `improve-target --target arc` accepts a playbook gain |
| 2 | Empty playbook | scores 0/held-in; no crash |
| 3 | Bad input / kernel write | frozen prefix throws |
| 4 | Regression | HostPort / decideAccept tests still pass |
| 5 | Auth fail | N/A — no auth |
| 6 | generic-agentic | second kind `agentic-system` accepts without coding fixtures |


---

## Escalate

After 3 failures, stop dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `test(target-port): boot and trial log` | this node | Stop command |

---

## Do not

- Commit from a nested Task
- Write outside write paths
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files. Checker: pass/exit. Lead updates `T1.state.json`.
