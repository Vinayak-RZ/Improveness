# Loop plan — `D0` — docs-in

> Parent: [LOOP_GRAPH.md](../LOOP_GRAPH.md)

| Field | Value |
|-------|-------|
| **Node id** | `D0` |
| **Job** | Confirm existing docs are enough to build TargetPort. |
| **Wave** | 0 |
| **Depends on (data)** | previous wave artifacts |
| **Write paths** | `docs/planning/GATE_0.md` |
| **Read paths** | `docs/plans/p5-target-port.md`, `docs/planning/GATE_0.md` |
| **Maker type** | lead |
| **Maker model** | inherit |
| **Checker type** | lead Stop command (no nested Task in this worker) |
| **Checker model** | inherit |
| **Max rounds** | 3 |
| **State** | [`D0.state.json`](D0.state.json) |
| **Isolation** | path-ownership |
| **Companion skills** | ponytail |

---

## Stop (required)

```text
test -f docs/planning/GATE_0.md && test -f docs/methods/host-port.md && test -f harness/omp/drivers/self-harness.ts
```

Exit 0 = pass.

---

## Objective

Confirm existing docs are enough to build TargetPort.

## Non-goals

- Extra features not in PRODUCT P0
- Nested graph spawn

---

## Contract

**Input:**

```json
{ "node_id": "D0", "findings": [] }
```

**Maker output:**

```json
{ "files_touched": [], "notes": "" }
```

**Checker output:**

```json
{ "pass": false, "command": "test -f docs/planning/GATE_0.md && test -f docs/methods/host-port.md && test -f harness/omp/drivers/self-harness.ts", "exit_code": 1, "findings": [] }
```



---

## Escalate

After 3 failures, stop dependents.

---

## Commits (this node only)

| # | Commit | Contents | Gate |
|---|--------|----------|------|
| | `docs(plan): TargetPort nawab + loop graph` | this node | Stop command |

---

## Do not

- Commit from a nested Task
- Write outside write paths
- Use Cursor `/loop` timers

---

## Return to graph

Maker: files. Checker: pass/exit. Lead updates `D0.state.json`.
